function [output] = L0Norm(data)
%L0NORM Takes the L0-norm of a given vector

[x y]=size(data);
if((x~=1)&&(y~=1))
    disp('Input is not a vector')
    return;
end
if(x==1)
    data=data';
end

output=[];

for i=1
output=norm(data,0);
end
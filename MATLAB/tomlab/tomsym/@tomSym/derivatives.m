% tomSym/derivatives - The symbolic derivatives of a tomSym object
%
% df_dxs = derivatives(f,xlist) computes the derivatives of a tomSym object
% f with respect to a list of symbols xlist.
%
% df_dxs = derivatives(f,xlist,'forwards') uses forwards instead of
% backwards accumulation. 
%
% See also: derivative
